# ============================================================
# 🚀 GOODIES CROWDFUNDING PROJECT - FINAL .GITIGNORE
# Backend (Node.js / Express / MongoDB / Stripe / PayPal)
# Frontend (HTML / CSS / JS served from /public)
# ============================================================

# --- Node dependencies ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Environment / secrets ---
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# --- Logs ---
logs/
*.log
log/
pids/
*.pid
*.seed
*.pid.lock

# --- System files ---
.DS_Store
Thumbs.db

# --- Build and cache directories ---
dist/
build/
.cache/
.next/
out/
coverage/

# --- IDE / Editor ---
.vscode/
.idea/
*.swp
*.swo

# --- Uploads / Generated assets ---
public/uploads/
server/uploads/
tmp/
temp/
uploads/

# --- OS / Miscellaneous ---
*.tmp
*.bak
*.old
*.orig
*.rej

# --- Git Large File Storage (if configured) ---
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# --- Misc generated files ---
*.tgz
*.log
*.pem
*.crt
*.key

# --- PM2 / Process manager logs ---
.pm2/

# --- Docker / Containerization ---
Dockerfile
docker-compose.yml
.dockerignore

# --- Local testing / coverage / cache ---
nyc_output/
coverage/
.env.test

# --- Firebase / GCP / AWS credentials ---
*.json
firebase-debug.log

# --- Private keys / configs ---
config/*.json
config/*.pem
config/*.key
